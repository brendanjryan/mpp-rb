# Tempo Charge Example

One-shot Tempo payments over HTTP using the MPP charge flow.

## Setup

```sh
cd examples/tempo_charge
bundle install
ruby app.rb
```

## Usage

```sh
curl -i http://localhost:4567/
curl -i http://localhost:4567/weather
```
